# One Favourites, not many playlists

A listener saves albums into a single Favourites set. Multiple named lists are not the product. The existing playlists API/UI is leftover shape, not the domain: Favourites is the word, album grain, one set per listener.
